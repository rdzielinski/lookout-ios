import SwiftUI

// HistoryView has been replaced by ScanHistoryView.
// This file is intentionally minimal to avoid a redeclaration conflict.
// The ScanHistoryView in ScanHistoryView.swift is the canonical history UI.
// If you have any NavigationLink or .sheet pointing to HistoryView,
// update them to point to ScanHistoryView(viewModel:) instead.
